# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## 仓库当前状态

**这是一个原型阶段仓库，还没有构建系统**：只有 `Context.md`（项目交接文档，必读）和 `UI/` 下两份独立的 HTML 原型（`Pet Arena __ Battle.html`、`Pet Arena __ Terminal.html`）。**没有 package.json、没有 npm/test/build/lint 命令**——任何"运行测试"或"启动 dev server"的请求在 Sprint 1 完成 Next.js 初始化前都不适用。

预览原型直接用浏览器打开 HTML 文件即可；没有打包步骤。`UI/*_files/` 目录是浏览器保存页面时附带的字体资源（Google Fonts CSS），不要手动编辑。

## 大局架构

### Pet JSON 是中心数据契约
所有功能（卡片渲染、对战引擎、Gallery、分享）都消费同一个 pet JSON 对象。Schema 定义在 `Context.md` §4.1，关键字段：
- `stats`：六维 1-100（HP/ATK/DEF/SPD/INT/LUK），打分标准在 §5.2
- `faction_affinity`：三阵营独立 0-100（chat/cowork/code），**基于产品名出现频率，不是任务类型**——这是 v1 prompt 暴露过的高频错误
- `skills`：固定 4 个，威力分布必须是 `1×ult(90+) + 1×main(80-89) + 2×std(60-79)`
- `elements`：从 五元素（火/水/土/雷/暗）+ 特殊"金" 中选 1-2 个，克制循环见 §5.3
- `generation_evidence`：**隐私红线字段，绝对不进入分享卡片**，仅本地预览显示

### 四派 LLM 是设计基础，不是数值平衡问题
Claude/ChatGPT/Gemini/DeepSeek 因为各自的"自我调用能力"不同（Context.md §3），各派 pet 的"出身阶级"是有意的不平等——这本身就是产品叙事，**不要尝试做数值平衡**。

### 隐私模型：本地优先
- JSON 解析、卡片渲染、对战计算全部跑在浏览器
- 服务端只存用户主动公开的 pet，且 evidence 已剥离
- 每个字段独立的 显示/隐藏/编辑 开关
- 卡片角标用 `petarena.xyz`，不是 "Generated by Claude"——品牌归属是产品决策，不是文案

### 对战引擎（在 `UI/Pet Arena __ Battle.html`）
是一个**自包含的、可工作的**对战原型，要移植到 React 时整体保留这套逻辑：
- 数值公式：`伤害 = power × (atk/100) × 2.4 × 元素倍率 × 暴击倍率 × 随机浮动 × (100/(100+def))`
- 暴击概率 `(luk/4 + 5)%`，暴击倍率 1.7，随机浮动 0.85-1.15
- 元素压制 1.4×，被压制 0.7×
- 速度判定先手，12 回合上限
- AI：HP <35% 偏向治疗；否则按 power 加权随机
- 已验证：Tessera vs Aether-7 胜率约 60%，平衡可接受

### 三种卡片风格的定位
- **Terminal**（当前已有原型）：CRT 扫描线、phosphor 绿光、VT323 字体——开发者社群受众
- **Pixel**（待开发）：GBA 卡牌风、16×16 SVG sprite——大众怀旧
- **Editorial**（待开发）：Fraunces 衬线 + 雷达图——design Twitter 受众

风格策略未拍板（Context.md §10.1），并存让用户选是当前倾向。

## 开发路径

完整 Sprint 计划见 `Context.md` §12.1。当前位置：**Sprint 1 未启动**。下一步是 Next.js 初始化 + 把 HTML 原型拆成 React 组件。

技术栈预定：Next.js + Tailwind + html2canvas/satori 导出 PNG + Supabase/Turso 仅存公开 pet + Vercel + Cloudflare Pages 双部署（解决 GFW）。

## 决策灵魂三问（每次改动前自问）

1. 这个改动让用户更愿意分享吗？
2. 这个改动让 evidence 更安全吗？
3. 这个改动让作者署名更清晰吗？

三个都不是 → 这个改动可能不该做。
